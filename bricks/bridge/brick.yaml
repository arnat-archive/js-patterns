name: js-pattern-bridge
description: Bridge pattern is about preferring composition over inheritance. Implementation details are pushed from a hierarchy to another object with a separate hierarchy.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.47 <0.1.0"
