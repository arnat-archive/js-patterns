name: js-pattern-iterator
description: It presents a way to access the elements of an object without exposing the underlying presentation.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.47 <0.1.0"
