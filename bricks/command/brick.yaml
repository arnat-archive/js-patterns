name: js-pattern-command
description: Allows you to encapsulate actions in objects. The key idea behind this pattern is to provide the means to decouple client from receiver.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.47 <0.1.0"
