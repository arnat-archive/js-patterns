name: js-pattern-decorator
description: Decorator pattern lets you dynamically change the behavior of an object at run time by wrapping them in an object of a decorator class.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.47 <0.1.0"
